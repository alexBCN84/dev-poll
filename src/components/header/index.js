import React from 'react';
import Logo from '../../atoms/logo';
import './index.css';

const Header = () => (
    <header className="app-header">
        <Logo />
    </header>
);

export default Header;